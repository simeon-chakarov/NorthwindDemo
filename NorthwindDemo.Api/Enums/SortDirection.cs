namespace NorthwindDemo.Api.Enums
{
    /// <summary>
    /// Specifies the direction in which results are sorted.
    /// </summary>
    public enum SortDirection
    {
        /// <summary>Sort from lowest to highest (default).</summary>
        Ascending,

        /// <summary>Sort from highest to lowest.</summary>
        Descending
    }
}
